from flask import Flask, render_template, request
from EmotionDetection.emotion_detection import emotion_detector

# Instantiate the Flask web application instance
app = Flask("Emotion Detector")

@app.route("/emotionDetector")
def emot_detector():

    # Retrieve the text input parameter submitted from the frontend UI form
    text_to_analyze = request.args.get('textToAnalyze')
    
    # Run the emotion analysis engine package
    response = emotion_detector(text_to_analyze)
    
    # Extract scores and dominant emotion mapping from the output dict profile
    anger = response['anger']
    disgust = response['disgust']
    fear = response['fear']
    joy = response['joy']
    sadness = response['sadness']
    dominant_emotion = response['dominant_emotion']
    
    # Return the target customer requirement string presentation layer format
    return (
        f"For the given statement, the system response is "
        f"'anger': {anger}, 'disgust': {disgust}, 'fear': {fear}, "
        f"'joy': {joy} and 'sadness': {sadness}. "
        f"The dominant emotion is <b>{dominant_emotion}</b>."
    )

@app.route("/")
def render_index_page():

    return render_template('index.html')

if __name__ == "__main__":
    app.run(host="0.0.0.0", port=5000)